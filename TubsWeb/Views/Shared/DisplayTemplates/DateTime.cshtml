@model DateTime?

@{
    string format = (string)ViewData["format"];
    if (null == format || "".Equals(format.Trim())) {
        format = "g";
    }
}

@Html.Encode( Model.HasValue ? Model.Value.ToString(format) : "N/A")