@model IEnumerable<Spc.Ofp.Tubs.DAL.Entities.PollutionEvent>

@* For now, create this in the View. *@
@{
    IEnumerable<Tuple<string, string>> crumbs = new List<Tuple<string, string>>()
    {
        Tuple.Create(Url.Action("Index", "Trip"), "Trips"),
        Tuple.Create(Url.Action("Details", "Trip", new { tripId = ViewBag.TripId }), ViewBag.TripNumber as string),
        Tuple.Create("#", "GEN-6 Events")
    };
}

@{ Html.RenderPartial("_BreadCrumbs", crumbs); }

@{ Html.RenderPartial("_PollutionEvents", Model); }

@if (!ViewBag.IsReadOnly)
{
    @Html.ActionLink("Add GEN-6 Event", "Add", new { tripId = ViewBag.TripId }, new { @class = "btn btn-primary" })
}
