@model TubsWeb.Models.Gen1ViewModel

<h2>@ViewBag.Title</h2>

<p>
<a href='@Url.RouteUrl("TripDetails", new { tripId = ViewBag.TripId })'>Back to Trip</a>
</p>

<fieldset>
    <legend>Vessel or Aircraft Sightings</legend>
    @{ Html.RenderPartial("_Sightings", Model.Sightings); }
</fieldset>

<fieldset>
    <legend>Fish transferring, Fish dumping, Bunkering by Observer's Vessel</legend>
    @{ Html.RenderPartial("_Transfers", Model.Transfers); }
</fieldset>

<p>
<a href='@Url.RouteUrl("TripDetails", new { tripId = ViewBag.TripId })'>Back to Trip</a>
</p>